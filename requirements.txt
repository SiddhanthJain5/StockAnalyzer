# Live market-data fetch for the /analyze-stock and /analyze-fund pipelines (Stage 0).
# Install once:  python3 -m pip install -r requirements.txt
yfinance>=0.2.40
# Used for the screener.in India-fundamentals scrape (also pulled in by yfinance).
requests>=2.31
beautifulsoup4>=4.12

# Web app (webapp/) - FastAPI backend; the analysis stages are a rules-based
# scoring engine (webapp/scoring.py), no paid API required.
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
